import 'package:flutter/material.dart';

// Define the gradient
const LinearGradient appGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [primaryBackground, primaryBackgroundLight],
);

// Primary Colors
const Color primaryBackground = Color(0xFF33691E);
const Color primaryBackgroundLight = Color(0xFF9CCC65);

// Secondary Colors
const Color skyBlue = Color(0xFF03A9F4);
const Color teal = Color(0xFF00897B);

// Neutral Colors
const Color offWhite = Color(0xFFFAFAFA);
const Color charcoalGrey = Color(0xFF37474F);

// Accent Color
const Color sunsetOrange = Color(0xFFFF7043);
